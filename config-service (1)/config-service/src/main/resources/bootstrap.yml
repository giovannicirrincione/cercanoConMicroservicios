server:
  port: 8080
spring:
  security:
      basic:
        enabled: false
  cloud:
    config:
      server:
        git:
          uri: http://github.com/giovannicirrincione/cercanoConMicroservicios.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}

